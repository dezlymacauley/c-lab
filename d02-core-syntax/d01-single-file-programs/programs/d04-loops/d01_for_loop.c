/*
    ABOUT: `for` loop

    This is used when you want to perform some action for every element 
    in an aggregate data type like a fixed-sized array.

    The syntax is:

    for (size_t i = index to start at; i < n; i++) {
        Code that should be run.
    }

    i is a variable to represent the value of the index.

    n is a variable to represent what index to stop at

    `i++` tells the `for` loop to move to the next element in the list.
*/

#include <stddef.h>
#include <stdio.h>

int main(void) {

    // indexes:             0,  1,  2,  3,  4,  5,  6,  7
    int player_scores[8] = {15, 24, 11, 98, 61, 19, 34, 10};


    // for () {
    //
    // }

    return 0;
}
