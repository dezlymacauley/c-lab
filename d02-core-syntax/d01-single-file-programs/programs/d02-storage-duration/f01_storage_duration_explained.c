/*
    ABOUT: Storage Duration Explained

    In C, the term `storage duration` refers to how long a variable remains
    valid for use.

    Storage duration looks at the following:
    1. When is memory assigned to store the contents of that variable?
    This memory is assigned by either the compiler or the operating system,
    depending on how the variable is declared.

    2. When is the memory assigned to that variable released back to the
    program or the operating system?

    The storage duration of a variable can be classified into one of these
    categories:
    1. automatic
    2. static
    3. allocated
    4. thread
*/

int main(void) {
}
